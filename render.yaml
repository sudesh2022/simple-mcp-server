services:
  - type: web
    name: simple-mcp-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn mcp_sse_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
